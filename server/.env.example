DATABASE_URL="postgresql://aurora_app:aurora_app_dev_pw@localhost:5432/aurora_pm"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
# Optional — only needed for "Create project from a contract (PDF)". Get a key at
# console.anthropic.com. Without it, that one feature returns a friendly 503; everything
# else in the app works fine.
ANTHROPIC_API_KEY=""
# Optional — only needed to show live ticket status on projects with a TeamSupport ticket #
# linked. Find both values in TeamSupport under Setup > API. Without them, that section
# returns a friendly 503; everything else in the app works fine.
TEAMSUPPORT_ORG_ID=""
TEAMSUPPORT_API_TOKEN=""
# Optional — only set this if the default deep-link URL below stops working (TeamSupport has
# changed this path before). Must contain the literal "{ticketNumber}" placeholder.
TEAMSUPPORT_TICKET_URL_TEMPLATE=""
